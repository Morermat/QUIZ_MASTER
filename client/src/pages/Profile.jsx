import {useEffect,useState} from 'react';import {api} from '../api';import {useAuth} from '../context/AuthContext';import {useNavigate} from 'react-router-dom';
export default function Profile(){const {setUser}=useAuth(),nav=useNavigate();const [data,setData]=useState(null),[name,setName]=useState(''),[avatar,setAvatar]=useState(''),[error,setError]=useState(''),[saving,setSaving]=useState(false);
 const load=async()=>{try{const r=await api.get('/profile');setData(r.data);setName(r.data.user.name);setAvatar(r.data.user.avatar_url||'')}catch(e){setError(e.response?.data?.error||'Не удалось загрузить профиль')}};useEffect(()=>{load()},[]);
 const file=e=>{const f=e.target.files?.[0];if(!f)return;if(!f.type.startsWith('image/'))return setError('Выберите изображение');if(f.size>1024*1024)return setError('Аватар должен быть меньше 1 МБ');const r=new FileReader();r.onload=()=>setAvatar(String(r.result));r.readAsDataURL(f)};
 const save=async()=>{setSaving(true);setError('');try{const r=await api.patch('/profile',{name,avatar_url:avatar});setUser(r.data.user);sessionStorage.setItem('user',JSON.stringify(r.data.user));await load()}catch(e){setError(e.response?.data?.error||'Не удалось сохранить')}finally{setSaving(false)}};
 if(error&&!data)return <div className="p-8"><p className="text-red-500">{error}</p><button onClick={()=>nav('/dashboard')}>Назад</button></div>;if(!data)return <div className="p-8">Загрузка...</div>;const s=data.stats;return <div className="max-w-3xl mx-auto p-6"><h1>Профиль</h1>{error&&<p className="text-red-500">{error}</p>}<div className="flex gap-4 items-center mb-6"><img src={avatar} alt="Аватар" className="w-24 h-24 rounded-full object-cover"/><div><input value={name} maxLength={40} onChange={e=>setName(e.target.value)} className="px-3 py-2 border rounded bg-[var(--bg)]"/><input type="file" accept="image/*" onChange={file}/><button disabled={saving} onClick={save}>{saving?'Сохранение...':'Сохранить'}</button></div></div><div className="grid grid-cols-2 md:grid-cols-4 gap-3 mb-6">{[['Игр',s.gamesPlayed],['Побед',s.wins],['Правильных',s.correctAnswers],['Процент',`${s.correctPercent}%`]].map(([a,b])=><div className="p-4 border rounded" key={a}><b className="text-2xl">{b}</b><div>{a}</div></div>)}</div><h2>История игр</h2>{s.history.length?s.history.map((h,i)=><div key={`${h.date}-${i}`} className="p-3 border-b">{h.quizTitle} — {h.score} балла, место {h.place}, {new Date(h.date).toLocaleString()}</div>):<p>История пока пустая</p>}<button className="mt-4" onClick={()=>nav('/dashboard')}>Назад</button></div>}
